import {createContext} from "react";

export default createContext(null);  //as default it is null means no data
//this will store the info about user that can be used accross all the application  

